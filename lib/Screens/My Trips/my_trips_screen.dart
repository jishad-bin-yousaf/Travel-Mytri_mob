import 'package:flutter/material.dart';
import 'package:travel_mytri_mobile_v1/bottom_navigation.dart';

class ScreenMyTrips extends StatelessWidget {
  const ScreenMyTrips({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(), bottomNavigationBar: const BottomNavigation(), body: const Scaffold());
  }
}
